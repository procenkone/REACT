import React from 'react';

const Posts = ({posts}) => {
    console.log(posts[0].id)
    return (
        <div>
            {/*{posts.map(item=>{item.id})}*/}

        </div>
    );
};

export default Posts;