import React from 'react';

const Form = () => {
    return (
        <div>
            <form>
                <input name={'todo'} type={'text'}/>
                <button>save</button>
            </form>

        </div>
    );
};

export default Form;